#include <stdio.h>
/**
 * main - Entry point
 * @argc: first operand
 * @argv: second operand
 * Return: Always 0 (Success)
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
