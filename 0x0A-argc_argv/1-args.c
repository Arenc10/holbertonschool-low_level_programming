#include <stdio.h>
/**
 * main - Entry point
 * @argc: First operand
 * @argv: Second operand
 * Return: Always 0 (Success)
 */
int main(int argc, char __attribute__((unused)) *argv[])
{
	printf("%d\n", argc);
	return (0);
}
