#include "main.h"
/**
 * _abs - a function that shows the absolute value
 * @a: an integer that takes a value
 * Return: Always 0 (Successful)
 */
int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (-a);
}
